<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                android:paddingBottom="@dimen/activity_vertical_margin"
                tools:context="org.bailedout.prevail.android.example.ui.TodoListFragment"
                android:gravity="center">

    <ListView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/list"
            android:layout_alignParentTop="true"
            android:layout_above="@+id/addEditText"/>
    <CheckBox
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/addButton" android:src="@android:drawable/ic_input_add"
            android:background="@android:color/transparent"
            android:button="@drawable/add_checkbox" android:checked="false" android:layout_alignLeft="@+id/list"
            android:layout_alignBaseline="@+id/addEditText"/>
    <EditText
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:inputType="textPersonName"
            android:id="@+id/addEditText"
            android:layout_alignRight="@+id/list" android:visibility="invisible" android:hint="@string/do_what"
            android:layout_toRightOf="@+id/addButton" android:layout_alignParentBottom="true"
            />
</RelativeLayout>
